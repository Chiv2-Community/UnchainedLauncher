<UserControl x:Class="UnchainedLauncher.GUI.Views.Servers.ServersTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UnchainedLauncher.GUI.Views.Servers"
             xmlns:designInstances="clr-namespace:UnchainedLauncher.GUI.Views.Servers.DesignInstances"
             d:DataContext="{d:DesignInstance designInstances:ServersTabDesignVM}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Split button variants: square inner edge, rounded outer edge -->
        <Style x:Key="Button.Success.SplitLeft" TargetType="Button" BasedOn="{StaticResource Button.Success}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="4,0,0,4"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="4" HorizontalAlignment="Center" VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.95" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFADB5BD" />
                                <Setter Property="Foreground" Value="#FFE9ECEF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Button.Danger.SplitRight" TargetType="Button" BasedOn="{StaticResource Button.Danger}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="0,4,4,0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="4" HorizontalAlignment="Center" VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.95" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFADB5BD" />
                                <Setter Property="Foreground" Value="#FFE9ECEF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Server selector -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <ComboBox
                Grid.Column="0"
                ItemsSource="{Binding ServerConfigs}"
                SelectedItem="{Binding SelectedConfiguration}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding Path=Name, Mode=OneWay}" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="8 0 0 0">
                <!-- Split buttons: inner edges squared to appear as one control -->
                <Button Width="50"
                        Command="{Binding CreateNewConfigCommand}"
                        Style="{StaticResource Button.Success.SplitLeft}"
                        Content="+"
                        Margin="8 0 -2 0" />
                <Button Width="50"
                        Command="{Binding RemoveConfigurationCommand}"
                        Style="{StaticResource Button.Danger.SplitRight}"
                        Content="-"
                        Margin="0" />
            </StackPanel>
        </Grid>
        <!-- Configuration editor & launch buttons (scrollable content, fixed buttons at bottom) -->
        <Grid Grid.Row="1" Visibility="{Binding ConfigurationEditorVisibility}" Margin="0, 8, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="8" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <!-- Scrollable configuration content -->
            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          CanContentScroll="True">
                <local:ServerTemplate DataContext="{Binding SelectedConfiguration}" />
            </ScrollViewer>
            <!-- Launch buttons fixed at bottom -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding LaunchServerCommand}" Style="{StaticResource Button.Primary}">Launch server</Button>
                <Button Grid.Column="2" Command="{Binding LaunchHeadlessCommand}"
                        Style="{StaticResource Button.Secondary}">
                    Launch Headless Server
                </Button>
            </Grid>
        </Grid>


        <!-- Running server stuff-->
        <Grid Grid.Row="1" Visibility="{Binding LiveServerVisibility}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <local:Server DataContext="{Binding SelectedServer}" Grid.Row="0" />
            <Button Grid.Row="1"
                    Command="{Binding ShutdownServerCommand}"
                    DockPanel.Dock="Bottom"
                    Style="{StaticResource Button.Danger}">
                Shut Down
            </Button>
        </Grid>
    </Grid>
</UserControl>