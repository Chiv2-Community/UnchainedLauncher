<UserControl x:Class="UnchainedLauncher.GUI.Views.Servers.Sections.LastTeamStandingSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ini="clr-namespace:UnchainedLauncher.GUI.ViewModels.ServersTab.IniSections"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=ini:LtsGameModeSectionVM}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="8">
            <Border BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="6" Padding="8">
                <StackPanel>
                    <TextBlock Text="LTS" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="PreCountdownDelay" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBox Text="{Binding PreCountdownDelay}" Grid.Column="1" Width="80" />

                        <TextBlock Text="RoundsToWin" Grid.Column="3" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBox Text="{Binding RoundsToWin}" Grid.Column="4" Width="80" />
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
