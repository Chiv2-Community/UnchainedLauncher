<UserControl x:Class="UnchainedLauncher.GUI.Views.ServersTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnchainedLauncher.GUI.Views"
             xmlns:instances="clr-namespace:UnchainedLauncher.GUI.Views.DesignInstances"
             d:DataContext="{x:Static instances:ServersTabInstances.DEFAULT}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl>
            <TabItem Header="Server">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox 
                                Grid.Column="0"
                                ItemsSource="{Binding ServerTemplates}" 
                                SelectedItem="{Binding SelectedTemplate}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock><Run Text="{Binding Path=Form.Name, Mode=OneWay}"/></TextBlock>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <Button Command="{Binding Add_Template_Command}">
                                    <Run Text=" + "/>
                                </Button>
                                <Button Command="{Binding Remove_Template_Command}">
                                    <Run Text="  -  "/>
                                </Button>
                            </StackPanel>
                        </Grid>
                        
                        <local:ServerTemplate Grid.Row="1" DataContext="{Binding SelectedTemplate}"/>
                    </Grid>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0">Launch server</Button>
                        <Button Grid.Column="2">Launch Headless Server</Button>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Running Servers">
                <local:Servers />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
