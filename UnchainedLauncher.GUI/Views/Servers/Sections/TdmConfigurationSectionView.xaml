<UserControl x:Class="UnchainedLauncher.GUI.Views.Servers.Sections.TdmConfigurationSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:designInstances="clr-namespace:UnchainedLauncher.GUI.Views.Servers.DesignInstances"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance designInstances:ServerConfigurationDesignVM}">

    <UserControl.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="8">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="TDM Time Limit" Grid.Row="0" Grid.Column="0" Style="{StaticResource FormLabel}" ToolTip="Duration of a TDM match in seconds. Leave blank for default." />
                <TextBox Text="{Binding TDMTimeLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource NullableIntConverter}}"
                         Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" ToolTip="Duration of a TDM match in seconds. Leave blank for default." />

                <TextBlock Text="TDM Ticket Count" Grid.Row="0" Grid.Column="3" Style="{StaticResource FormLabel}" ToolTip="Number of tickets each team receives at the start of a match. Leave blank for default." />
                <TextBox Text="{Binding TDMTicketCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource NullableIntConverter}}"
                         Grid.Row="0" Grid.Column="4" HorizontalAlignment="Stretch" ToolTip="Number of tickets each team receives at the start of a match. Leave blank for default." />
            </Grid>
        </StackPanel>
    </StackPanel>
</UserControl>
