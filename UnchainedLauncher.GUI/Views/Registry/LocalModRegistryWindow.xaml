<Window x:Class="UnchainedLauncher.GUI.Views.Registry.LocalModRegistryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:UnchainedLauncher.GUI.Views.Registry"
        xmlns:instances="clr-namespace:UnchainedLauncher.GUI.Views.DesignInstances"
        xmlns:registry="clr-namespace:UnchainedLauncher.GUI.ViewModels.Registry"
        mc:Ignorable="d"
        Title="{Binding Registry.Name}" Height="450" Width="800"
        d:DataContext="{x:Static instances:LocalModRegistryConfigWindowViewModelInstances.DEFAULT}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type registry:RegistryModVM}">
            <StackPanel>
                <TextBlock>
                    <Run Text="{Binding Mod.LatestManifest.Organization, Mode=OneWay}"/>
                    /
                    <Run Text="{Binding Mod.LatestManifest.Name, Mode=OneWay}"/>
                </TextBlock>
                <ListBox ItemsSource="{Binding Releases}"/>
                <Button Command="{Binding AddReleaseCommand}">Add Release</Button>
            </StackPanel>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type registry:RegistryReleaseVM}">
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                    <Run Text="{Binding Release.Version, Mode=OneWay}"/>
                </TextBlock>
                <Button Command="{Binding EditCommand}">Edit</Button>
                <Button Command="{Binding DeleteCommand}">Delete</Button>
            </StackPanel>
            
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding Mods, Mode=OneWay}"/>
        <Button Grid.Row="1" Command="{Binding AddReleaseCommand}">Add</Button>
    </Grid>
</Window>
